# 贡献指南

感谢您有兴趣为 SAST Readium 做出贡献！本指南将帮助您入门。

## 入门

### 前置要求

- Node.js 20.x 或更高版本
- pnpm 8.x 或更高版本
- Git
- （桌面开发）Rust 1.70+

### 设置

1. 在 GitHub 上 Fork 仓库
2. 克隆您的 Fork：
   ```bash
   git clone https://github.com/YOUR_USERNAME/sast-readium-web.git
   cd sast-readium-web
   ```
3. 安装依赖：
   ```bash
   pnpm install
   ```
4. 创建功能分支：
   ```bash
   git checkout -b feature/your-feature-name
   ```

## 开发工作流

### 运行应用

```bash
# Web 开发
pnpm dev

# 桌面开发
pnpm tauri dev
```

### 代码质量

提交前，确保您的代码通过所有检查：

```bash
# 代码检查
pnpm lint

# 类型检查
pnpm typecheck

# 测试
pnpm test

# 格式化
pnpm format
```

### Pre-commit 钩子

项目使用 Husky 进行 pre-commit 钩子，自动：

- 运行 ESLint 并自动修复
- 使用 Prettier 格式化代码

## 进行更改

### 编码规范

- 所有代码使用 TypeScript
- 遵循现有代码风格
- 使用有意义的变量和函数名
- 为复杂逻辑添加注释
- 保持函数小而专注

### 组件指南

- 使用带 hooks 的函数组件
- 使用 TypeScript 接口定义 prop 类型
- 使用 `cn()` 进行条件类名
- 遵循 shadcn/ui 模式的 UI 组件

## 提交更改

### 提交消息

遵循 [Conventional Commits](https://www.conventionalcommits.org/)：

```
type(scope): description

[可选正文]

[可选页脚]
```

**类型：**

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档
- `style`: 格式化（无代码更改）
- `refactor`: 代码重构
- `test`: 添加测试
- `chore`: 维护

**示例：**

```
feat(pdf): 添加页面旋转支持
fix(annotations): 解决高亮颜色持久化问题
docs(readme): 更新安装说明
```

### Pull Requests

1. 推送您的分支：

   ```bash
   git push origin feature/your-feature-name
   ```

2. 在 GitHub 上打开 Pull Request

3. 填写 PR 模板：
   - 描述您的更改
   - 链接相关 issues
   - 为 UI 更改添加截图
   - 列出测试步骤

4. 等待审查

### PR 要求

- 所有 CI 检查必须通过
- 需要代码审查批准
- 无合并冲突
- 新功能需要测试
- 如需要则更新文档

## 测试

### 编写测试

- 将测试与源文件放在一起
- 使用描述性测试名称
- 测试行为，而非实现
- 使用 React Testing Library 查询

### 运行测试

```bash
# 所有测试
pnpm test

# 监视模式
pnpm test:watch

# 覆盖率
pnpm test:coverage
```

## 文档

### 更新文档

文档在 `docs/` 目录中，使用 MkDocs。

```bash
# 本地服务文档
mkdocs serve

# 构建文档
mkdocs build
```

## 社区

### 行为准则

保持尊重和包容。我们遵循 [Contributor Covenant](https://www.contributor-covenant.org/)。

### 获取帮助

- 查看现有 issues 和讨论
- 在 GitHub Discussions 中提问
- 查阅文档

## 许可证

通过贡献，您同意您的贡献将在 MIT 许可证下授权。

---

感谢您为 SAST Readium 做出贡献！🎉
