# SAST Readium

一个现代化、功能丰富的 PDF 阅读器和注释应用程序，专注于性能和可用性。SAST Readium 将强大的 PDF 查看功能与全面的注释工具相结合，支持从单一代码库同时部署 Web 和桌面应用。

[English Documentation](./README.md)

## 特性

### PDF 查看与导航

- 📄 **多种查看模式**：单页、连续滚动、对开页
- 🔍 **灵活缩放**：50%-500% 缩放范围，支持页面适配、宽度适配和自动适配模式
- 🔄 **页面旋转**：支持 0°、90°、180°、270° 旋转
- 🎨 **主题支持**：明亮、暗黑、棕褐色模式，支持自动检测
- 📑 **文档大纲**：通过目录导航
- 🖼️ **缩略图侧边栏**：可视化页面导航，支持拖放重新排序
- 🎯 **全文搜索**：在整个文档中查找文本并高亮显示
- ⌨️ **键盘快捷键**：全面的键盘控制，提高导航效率

### 注释工具

- ✏️ **高亮显示**：多色文本高亮，支持自定义颜色
- 💬 **评论**：在任何页面添加文本注释
- 🎨 **绘图工具**：自由手绘，可调节笔画粗细
- 📐 **形状**：插入矩形、圆形和箭头
- 🖼️ **印章和签名**：添加自定义印章和数字签名
- ↩️ **撤销/重做**：完整的注释历史记录，支持撤销和重做

### 高级功能

- 🔖 **书签**：创建和管理带标题的自定义书签
- 🔊 **文本转语音**：大声朗读 PDF，可调节速度和音量
- 📝 **元数据编辑**：查看和编辑 PDF 元数据
- 🏷️ **水印**：为 PDF 文档添加水印
- 📁 **文件操作**：重命名、删除、在系统文件管理器中显示文件
- 📑 **多标签支持**：同时打开和管理多个 PDF
- 📦 **压缩包支持**：从 ZIP/RAR 压缩包中提取和阅读 PDF
- 🌍 **国际化**：支持英语和中文
- 📱 **触摸手势**：在触摸设备上支持双指缩放和滑动导航

### 技术栈

- ⚡️ **Next.js 16** 配合 App Router 和 React 19
- 🖥️ **Tauri 2.9** 用于原生桌面应用（Windows、macOS、Linux）
- 📄 **PDF.js** 高质量 PDF 渲染
- 🎨 **Tailwind CSS v4** 支持 CSS 变量和主题
- 🧩 **shadcn/ui** 组件库，基于 Radix UI 原语
- 📦 **Zustand** 状态管理，支持持久化
- 🌐 **react-i18next** 国际化
- 🧪 **Jest** 配合 React Testing Library 进行测试
- 🎯 **TypeScript** 提供类型安全
- 📱 **PWA 支持** 用于 Web 部署

## 前置要求

在开始之前，请确保已安装以下内容：

### Web 开发所需

- **Node.js** 20.x 或更高版本（[下载](https://nodejs.org/)）
- **pnpm** 8.x 或更高版本（推荐）

  ```bash
  npm install -g pnpm
  ```

### 桌面开发所需（额外要求）

- **Rust** 1.70 或更高版本（[安装](https://www.rust-lang.org/tools/install)）

  ```bash
  # 验证安装
  rustc --version
  cargo --version
  ```

- **系统依赖**（因操作系统而异）：
  - **Windows**：Microsoft Visual Studio C++ 生成工具
  - **macOS**：Xcode 命令行工具
  - **Linux**：参见 [Tauri 前置要求](https://tauri.app/v1/guides/getting-started/prerequisites)

## 安装

1. **克隆仓库**

   ```bash
   git clone https://github.com/NJUPT-SAST-CXX/sast-readium-web.git
   cd sast-readium-web
   ```

2. **安装依赖**

   ```bash
   pnpm install
   ```

3. **验证安装**

   ```bash
   # 启动 Web 开发服务器
   pnpm dev

   # 检查 Tauri 环境（用于桌面开发）
   pnpm tauri info
   ```

## 开发

### Web 应用开发

#### 启动开发服务器

```bash
pnpm dev
```

这将在 [http://localhost:3000](http://localhost:3000) 启动 Next.js 开发服务器。当您编辑文件时，页面会自动重新加载。

#### 关键开发文件

- `app/page.tsx` - 应用主入口点
- `components/pdf-viewer/` - PDF 查看器组件
- `lib/pdf-store.ts` - Zustand 状态管理
- `lib/pdf-utils.ts` - PDF.js 集成工具
- `locales/` - 国际化文件

### 桌面应用开发

#### 启动 Tauri 开发模式

```bash
pnpm tauri dev
```

此命令将：

1. 启动 Next.js 开发服务器
2. 启动 Tauri 桌面应用
3. 为前端和 Rust 代码启用热重载

#### Tauri 开发文件

- `src-tauri/src/lib.rs` - 自定义 Tauri 命令（文件操作、系统信息）
- `src-tauri/src/main.rs` - Rust 应用主入口点
- `src-tauri/tauri.conf.json` - Tauri 配置

## 可用脚本

### 前端脚本

| 命令 | 描述 |
|------|------|
| `pnpm dev` | 在 3000 端口启动 Next.js 开发服务器 |
| `pnpm build` | 构建生产环境的 Next.js 应用（输出到 `out/` 目录） |
| `pnpm start` | 启动 Next.js 生产服务器（在 `pnpm build` 之后） |
| `pnpm lint` | 运行 ESLint 检查代码质量 |
| `pnpm test` | 运行 Jest 测试 |
| `pnpm test:watch` | 以监视模式运行 Jest 测试 |
| `pnpm test:coverage` | 生成测试覆盖率报告 |

### Tauri（桌面）脚本

| 命令 | 描述 |
|------|------|
| `pnpm tauri dev` | 启动 Tauri 开发模式，支持热重载 |
| `pnpm tauri build` | 构建生产环境的桌面应用 |
| `pnpm tauri info` | 显示 Tauri 环境信息 |
| `pnpm tauri --help` | 显示所有可用的 Tauri 命令 |

### 添加 UI 组件（shadcn/ui）

```bash
# 添加新组件（例如 Card）
pnpm dlx shadcn@latest add card

# 添加多个组件
pnpm dlx shadcn@latest add button card dialog
```

## 项目结构

```text
sast-readium-web/
├── app/                          # Next.js App Router
│   ├── layout.tsx               # 根布局，包含 i18n 提供者
│   ├── page.tsx                 # 主应用页面（PDF 查看器 + 欢迎页）
│   └── about/page.tsx           # 关于页面，显示运行时信息
├── components/
│   ├── pdf-viewer/              # PDF 查看器组件
│   │   ├── pdf-viewer.tsx       # 主 PDF 查看器组件
│   │   ├── pdf-toolbar.tsx      # 桌面工具栏
│   │   ├── pdf-annotation-layer.tsx # 注释渲染
│   │   ├── pdf-drawing-layer.tsx    # 绘图工具
│   │   ├── pdf-outline.tsx      # PDF 目录
│   │   ├── pdf-bookmarks.tsx    # 用户书签
│   │   ├── pdf-tts-reader.tsx   # 文本转语音阅读器
│   │   └── ...                  # 更多 PDF 组件
│   ├── ui/                      # shadcn/ui 组件
│   ├── welcome-page/            # 欢迎屏幕
│   └── ...                      # 其他组件
├── lib/
│   ├── pdf-store.ts             # PDF 状态的 Zustand 存储
│   ├── pdf-utils.ts             # PDF.js 工具
│   ├── tauri-bridge.ts          # Tauri 命令包装器
│   ├── archive-utils.ts         # ZIP/RAR 解压
│   ├── i18n.ts                  # i18next 配置
│   └── ...                      # 其他工具
├── hooks/                       # 自定义 React hooks
├── locales/                     # i18n 翻译文件
│   ├── en/translation.json      # 英语
│   └── zh/translation.json      # 中文
├── src-tauri/                   # Rust 后端
│   ├── src/lib.rs               # 自定义 Tauri 命令
│   └── tauri.conf.json          # Tauri 配置
└── public/                      # 静态资源
```

## 配置

### 环境变量

在根目录创建 `.env.local` 文件以配置特定环境的变量：

```env
# 示例环境变量
NEXT_PUBLIC_APP_NAME=SAST Readium
```

**重要提示**：

- 只有以 `NEXT_PUBLIC_` 为前缀的变量会暴露给浏览器
- 切勿将 `.env.local` 提交到版本控制

### 路径别名

在 `tsconfig.json` 和 `components.json` 中配置：

```typescript
import { Button } from "@/components/ui/button"
import { usePDFStore } from "@/lib/pdf-store"
import { cn } from "@/lib/utils"
```

可用别名：

- `@/components` → `components/`
- `@/lib` → `lib/`
- `@/ui` → `components/ui/`
- `@/hooks` → `hooks/`

## 生产构建

### 构建 Web 应用

```bash
# 构建静态导出
pnpm build

# 输出目录：out/
# 将 out/ 目录部署到任何静态托管服务
```

构建会在 `out/` 目录中创建一个静态导出，已针对生产环境进行优化。

### 构建桌面应用

```bash
# 为当前平台构建
pnpm tauri build

# 输出位置：
# - Windows: src-tauri/target/release/bundle/msi/
# - macOS: src-tauri/target/release/bundle/dmg/
# - Linux: src-tauri/target/release/bundle/appimage/
```

构建选项：

```bash
# 为特定目标构建
pnpm tauri build --target x86_64-pc-windows-msvc

# 使用调试符号构建
pnpm tauri build --debug
```

## 部署

### Web 部署

应用程序可以作为静态网站部署到：

- **Vercel**（推荐）- 自动检测 Next.js 配置
- **Netlify** - 部署 `out/` 目录
- **GitHub Pages** - 托管静态导出
- **任何静态托管** - 上传 `out/` 目录

### 桌面部署

#### Windows

- 分发 `src-tauri/target/release/bundle/msi/` 中的 `.msi` 安装程序
- 用户运行安装程序以安装应用

#### macOS

- 分发 `src-tauri/target/release/bundle/dmg/` 中的 `.dmg` 文件
- 用户将应用拖到应用程序文件夹
- **注意**：对于 App Store 之外的分发，需要使用 Apple 开发者证书进行代码签名

#### Linux

- 分发 `src-tauri/target/release/bundle/appimage/` 中的 `.AppImage`
- 用户使其可执行并运行：`chmod +x app.AppImage && ./app.AppImage`
- 替代格式：`.deb`（Debian/Ubuntu）、`.rpm`（Fedora/RHEL）

详细说明请参见 [Tauri 分发指南](https://tauri.app/v1/guides/distribution/)。

## 测试

项目使用 Jest 配合 React Testing Library 进行测试：

```bash
# 运行所有测试
pnpm test

# 以监视模式运行测试
pnpm test:watch

# 生成覆盖率报告
pnpm test:coverage
```

- 测试文件：`*.test.ts`、`*.test.tsx`、`*.spec.ts`、`*.spec.tsx`
- 覆盖率阈值：70% 行数/语句，60% 分支/函数
- 测试环境：jsdom

## 键盘快捷键

| 快捷键 | 操作 |
|--------|------|
| `←` / `→` | 上一页/下一页 |
| `Home` / `End` | 第一页/最后一页 |
| `+` / `-` | 放大/缩小 |
| `0` | 重置缩放 |
| `F` | 切换全屏 |
| `Ctrl+F` | 搜索 |
| `Ctrl+B` | 切换书签 |
| `Ctrl+Z` / `Ctrl+Y` | 撤销/重做注释 |

在应用中按 `?` 查看所有键盘快捷键。

## 故障排除

### 常见问题

**端口 3000 已被占用**

```bash
# 终止使用端口 3000 的进程
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# macOS/Linux
lsof -ti:3000 | xargs kill -9
```

**Tauri 构建失败**

```bash
# 检查 Tauri 环境
pnpm tauri info

# 更新 Rust
rustup update

# 清理构建缓存
cd src-tauri
cargo clean
```

**PDF 渲染问题**

- 确保 PDF.js worker 在 `lib/pdf-utils.ts` 中正确配置
- 检查浏览器控制台的错误信息
- 尝试使用其他 PDF 文件以隔离问题

## 了解更多

### 文档

- [Next.js 文档](https://nextjs.org/docs) - 了解 Next.js 功能和 API
- [Tauri 文档](https://tauri.app/) - Tauri 官方文档
- [PDF.js 文档](https://mozilla.github.io/pdf.js/) - PDF.js API 参考
- [shadcn/ui](https://ui.shadcn.com/) - 组件库文档
- [Tailwind CSS](https://tailwindcss.com/docs) - Tailwind CSS 文档
- [Zustand](https://zustand-demo.pmnd.rs/) - Zustand 状态管理

### 相关项目

- [PDF.js](https://github.com/mozilla/pdf.js) - PDF 渲染引擎
- [react-pdf](https://github.com/wojtekmaj/react-pdf) - React PDF 查看器组件
- [Tauri](https://github.com/tauri-apps/tauri) - 桌面应用框架

## 贡献

欢迎贡献！请遵循以下步骤：

1. Fork 仓库
2. 创建功能分支（`git checkout -b feature/amazing-feature`）
3. 提交更改（`git commit -m 'feat: add amazing feature'`）
4. 推送到分支（`git push origin feature/amazing-feature`）
5. 打开 Pull Request

请确保您的代码：

- 遵循现有的代码风格（运行 `pnpm lint`）
- 为新功能包含测试
- 根据需要更新文档

## 许可证

本项目是开源的，采用 [MIT 许可证](LICENSE)。

## 团队

由南京邮电大学 SAST（Students' Association for Science and Technology）C++ 组开发。

- **组织**：[SAST-CXX](https://github.com/NJUPT-SAST-CXX)
- **网站**：[SAST](https://sast.fun/)

## 支持

如果您遇到任何问题或有疑问：

- 查看[故障排除](#故障排除)部分
- 在 [GitHub](https://github.com/NJUPT-SAST-CXX/sast-readium-web/issues) 上提出 issue
- 查阅上述文档链接

---

**注意**：本项目支持双重部署：

- **Web 模式**：渐进式 Web 应用，支持 Service Worker
- **桌面模式**：原生桌面应用，完全访问文件系统

两种模式共享相同的代码库，提供无缝的用户体验。
